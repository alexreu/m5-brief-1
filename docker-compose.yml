services:
  backend:
    build: ./backend
    container_name: carre-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: carre-frontend
    environment:
      BACKEND_URL: http://backend:8000
    ports:
      - "8501:8501"
    depends_on:
      - backend
